
# logstash.conf
input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "rag-chat-storage-service-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}